// primes-4-timer-unsafe-opt.l1com - Sieve of Eratosthenes
// primenum search
//
//
#include <intr.l1h>
(main func)
	(set int64 1 zero 0)
	(set byte 100000000 primes)
	// (set byte 1000000 primes)
	(set int64 1 limit 100000000)
	// (set int64 1 limit 10000)
	(set int64 1 one 1)
	(set int64 1 two 2)
	(set int64 1 i 2)
	(set int64 1 j 0)
	(set int64 1 z 1)
	(set int64 1 f 0)
	(set int64 1 a 0)
	(set int64 1 k 0)
	(set int64 1 n 0)
	(set int64 1 timer 0)
	(set string s timestr "time: ")
	(set byte 1 oneb 1)
	(set int64 1 limit_low 99999999)
	(set int64 1 mod_vect 0)
	(set int64 1 Pprimesptr 0)
	(mod_vect :math_vect_init !)
    (primes Pprimesptr pointer)
	// array primes is filled by zeroes
	//
	// search primes
	// vmath_array_init_byte
	(unsafe)
	(:memory_bounds_off !)
	(optimize-if)
	// start timer
	(:start_timer !)
	(two i =)
	(:search_primes)
	((i i *) n =)
	(((n limit <) f =) f if)
		(primes [ i ] a =)
		(((a zero ==) f =) f if)
			(i j m=)
            ((i j *) k =)
            (Pprimesptr k limit_low i one :vmath_array_init_byte !)
		(endif)
		((i one +) i =)
		(:search_primes jmp)
	(endif)
	// end timer
	(timer :stop_timer !)
    (timestr :print_s !)
    (timer :print_i !)
    (:print_n !)

	(two i =)
	(:print_primes)
	(((i limit <) f =) f if)
		(primes [ i ] a =)
		(((a zero ==) f =) f if)
            (i :print_i !)
            (:print_n !)
		(endif)
		((i one +) i =)
		(:print_primes jmp)
	(endif)
	(zero :exit !)
	(:memory_bounds_on !)
	(unsafe-end)
(funcend)
#include <math-lib-vect.l1h>
