# zerobuild makefile

[executable, shared, name = libl1vmraylib.so]
sources =  raylib.c, ../file/file-sandbox.c, ../lib/memory_bounds.c

includes = ../include, /usr/local/include

ccompiler = $CC
c++compiler = $CCPP

linker = ld
aflags = "cru"

cflags = "-O3 -fomit-frame-pointer -Wall"
lflags = "-shared -Wl,-rpath,/usr/local/lib -lraylib -lpthread"
