# zerobuild makefile

[executable, shared, name = libl1vmmqtt.so]
sources = mqtt.c, ../../../lib-func/string.c, ../lib/memory_bounds.c

includes = ../include, /usr/local/include

ccompiler = $CC
c++compiler = $CCPP

linker = ld
aflags = "cru"

cflags = "-fPIC -O0 -fomit-frame-pointer -Wall -g"
lflags = "-lpaho-mqtt3c -shared"
