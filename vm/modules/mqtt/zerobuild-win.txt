# zerobuild makefile

[executable, shared, name = libl1vmmqtt.so]
sources = mqtt.c ../../../lib-func/string.c, ../lib/memory_bounds.c

includes = ../include, /mingw64/include, /usr/include

ccompiler = $CC
c++compiler = $CCPP

linker = ld
aflags = "cru"

cflags = "-O3 -fomit-frame-pointer -Wall"
lflags = "-lpaho-mqtt3c -shared -lws2_32"
