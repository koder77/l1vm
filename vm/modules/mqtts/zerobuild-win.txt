# zerobuild makefile

[executable, shared, name = libl1vmmqtts.so]
sources = mqtts.c ../../../lib-func/string.c, ../lib/memory_bounds.c

includes = ../include, /mingw64/include, /usr/include

ccompiler = $CC
c++compiler = $CCPP

linker = ld
aflags = "cru"

cflags = "-O3 -fomit-frame-pointer -Wall"
lflags = "-lpaho-mqtt3cs -lssl -lcrypto -shared -lws2_32"
