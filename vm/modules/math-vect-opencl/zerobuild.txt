# zerobuild makefile

[executable, shared, name = libl1vmmathvect-opencl.so]
sources =  math-vect-int-opencl.c, math-vect-double-opencl.c, ../lib/memory_bounds.c

includes = ../include, /usr/local/include, /usr/include

ccompiler = $CC
c++compiler = $CCPP

linker = ld
aflags = "cru"

#cflags = "-fPIC -O3 -fomit-frame-pointer -Wall"
cflags = "-fPIC -shared -O2 -fno-tree-vectorize -fno-stack-protector"
lflags = "-shared -lm -lOpenCL"
