# zerobuild makefile

[executable, shared, name = libl1vmgenann.so]
sources = genann.c, genann-lib.c, ../file/file-sandbox.c

includes = ../include, /usr/local/include

ccompiler = $CC
c++compiler = $CCPP

linker = ld
aflags = "cru"

cflags = "-dynamiclib -fPIC -O3 -fomit-frame-pointer -Wall"
lflags = "-shared -lm"
