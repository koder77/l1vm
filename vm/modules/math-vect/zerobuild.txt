# zerobuild makefile

[executable, shared, name = libl1vmmathvect.so]
sources =  math-vect-int.c, math-vect-double.c, ../lib/memory_bounds.c

includes = ../include, /usr/local/include

ccompiler = $CC
c++compiler = $CCPP

linker = ld
aflags = "cru"

#cflags = "-fPIC -O3 -fomit-frame-pointer -Wall"
cflags = "-fPIC -shared -O2 -fno-tree-vectorize -mstack-alignment=8 -fno-stack-protector"
lflags = "-shared -lm"
