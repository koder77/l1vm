# zerobuild makefile

[executable, shared, name = libl1vmnet.so]
sources = net.c, ssl.c, ../../../lib-func/string.c, ../file/file-sandbox.c, ../lib/memory_bounds.c

includes = ../include, /usr/local/include, /System/Volumes/Data/opt/homebrew/include

ccompiler = $CC
c++compiler = $CCPP

linker = ld
aflags = "cru"

cflags = "-O3 -fomit-frame-pointer -Wall"
lflags = "-L/System/Volumes/Data/opt/homebrew/lib -lssl -lcrypto -bundle -undefined dynamic_lookup"
