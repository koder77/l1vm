Build AsmJIT on MSYS2
=====================
Run the copy.sh script to copy the patched make files to the directory above:

$ ./copy.sh

Then go one directory above:

$ cd ..

Finally run the build script:

$ ./build.sh

Now the AsmJIT library should be building.
