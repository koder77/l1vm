Here are some very usefull scripts.
With "build.sh" the modules are build in their modules/ sub directory.
And then copied to this modules directory.

With "install.sh" all modules here are copied to "/usr/local/lib"
That is way better as doing this manualy.
