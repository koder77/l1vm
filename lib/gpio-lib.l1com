(main func)
	(set int64 1 zero 0)
	(set int64 1 pin 17)
	(set int64 1 value 0)
	(set int64 1 fd 0)
	(zero :gpio_init call)
	(loadreg)
	(pin zero :gpio_pin_mode call)
	(loadreg)
	// (:setup_accel call)
	// (fd stpopi)
	// (loadreg)
	(:loop)
	(pin :gpio_digital_read call)
	(value stpopi)
	(loadreg)
	(4 value 0 0 intr0)
	(7 0 0 0 intr0)
	(:loop jmp)
	(fd :read_accel call)
	(loadreg)
	(:loop jmp)
	(255 zero 0 0 intr0)
(funcend)
#include <gpio-lib.l1h>
